#include <stdio.h>
int main() {
    float a = 5, b = 4, c = 3, d = 2, m, n;
    m = (4 * (a * b * b * d)) / (5 * c);
    n = ((3 * (a * a)) / b) + ((c - d) + 5);
    printf("m = %.3f\n", m);
    printf("n = %.2f", n);
    return 0;
}


Output:
m = 42.667
n = 24.75
