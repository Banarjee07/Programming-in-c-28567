// Using Printf()
# include <stdio.h>
	int main() 
	{ 
	printf (" Noakhali Ideal Polytechnic Institute.");
	return 0;
	}


Output:
Noakhali Ideal Polytechnic Institute.

//using mathematical operation
# include <stdio.h>
	int main() 
	{
	int x, y, z, P, Q; //P and Q are output variables//
	printf ("Enter value of x, y &z :");
	scanf ("%d%d%d",&x,&y,&z);
	P=(x*x)+(2*x*y)+(y*y);
	Q=(4*x*y*z)-(x-y);
	printf ("Value of P = %d ", P);
	printf ("Value of Q = % d", Q);
	return 0;
	} 


Output:
Enter value of x, y &z :4
5
3
Value of P = 81 Value of Q =  241


//Using putchar()
# include <stdio.h>
	void main()
	{
	char c ;
	putchar('A');
	putchar('B');
	}
Output:
AB


//Using Puts()
#include <stdio.h>
#include<string.h>
int main()
{
char str1[30];
char str2[30];
strcpy(str1, "Mohammed Mahbub Alam");
strcpy(str2, "Computer Technology");
puts(str1);
puts(str2);
return 0;
}


Output:
Mohammed Mahbub Alam
Computer Technology


