//Using integer
# include <stdio.h>
	int main() {
		int hours, days;
	printf (“Enter how many days : ”);
	scanf ("%d", &days);
	hours = 24 * days;
	printf ("\n %d days = %d hours", days, hours);
	return 0;
	}

Output:
Enter how many days : 12
 12 days = 288 hours


 //Using float
 # include <stdio.h>
	int main() 
	{
	float x, y ,result;
	printf (" Enter the value of x & y :");
	scanf ("%f %f", &x, &y);
	result = x + y;
	printf ("\nResult = %f", result);
	return 0;
	}


Output:
 Enter the value of x & y :10
20
Result = 30.000000


//Using Char
#include <stdio.h>
int main() 
{
char str[50];
printf("Enter your department Name: ");
scanf("%s",str);
printf("\nYou Entered: %s", str);
return 0;
}


Output:
Enter your department Name: Computer_Science
You Entered: Computer_Science



//using gets
#include <stdio.h>
int main() 
{
char str[50];
printf("Enter a string : ");
gets(str);
printf("You Entered: %s", str);
return 0;
}


Output:
Enter a string : Computer
You Entered: Computer


//Using getchar
# include <stdio.h>
	int main() 
	{
	char ch;
	printf ("Enter Your String :");
	while ((ch = getchar() )!= '\n')
	putchar (ch);
	printf (" \n Your Entered Above string. \n");
	return 0;
	}

 Output:
 Enter Your String :CST
CST 
 Your Entered Above string

